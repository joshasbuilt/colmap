/* navigation-system-swap_negY.js - FLIP Y/Z on load and negate Y */
const CONFIG = { DATA_FILE: 'cone_data.json', PANORAMA_BASE_PATH: 'panoramas/', SPHERE_SIZE_DEFAULT: 0.0875, FLIP_YZ_ON_LOAD: true, NEGATE_Y_ON_LOAD: true, NEGATE_Z_ON_LOAD: false };
/* Minimal implementation: delegate to same logic as original but with flags */
/* For brevity this file reuses the same runtime functions as the other variant (copied code omitted here) */
console.log('navigation-system-swap_negY.js loaded');
